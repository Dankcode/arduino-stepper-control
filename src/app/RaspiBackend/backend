"""Compatibility entrypoint.

The maintained Flask backend lives in backend.py. This wrapper keeps older
deployment docs and service commands that run `python3 backend` working.
"""

import runpy


if __name__ == "__main__":
    runpy.run_module("backend", run_name="__main__")
