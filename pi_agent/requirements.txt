flask
flask-cors
pyserial

# Raspberry Pi hardware packages are supplied by apt, not pip. Install these
# before creating the venv, then use --system-site-packages so this interpreter
# can import them:
#   sudo apt install -y python3-picamera2 python3-rpi.gpio
